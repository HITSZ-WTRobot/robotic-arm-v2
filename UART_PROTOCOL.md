# 机械臂上位机/下位机串口通讯协议

本文档对应当前固件实现，适用于上位机与下位机通过 `USART1` 的双向通信。

## 1. 串口参数

- 物理接口: `USART1`
- 波特率: `230400`
- 数据位: `8`
- 校验位: `None`
- 停止位: `1`
- 流控: `None`
- 通信方式: 全双工

## 2. 通用约定

### 2.1 字节序

协议中所有多字节字段均采用小端序。

- `uint16_t 0xAA55` 在线上字节序为: `55 AA`
- `float` 采用下位机当前平台的 IEEE 754 单精度 32 位表示，并按小端序发送

### 2.2 帧头

双向协议统一使用 2 字节帧头:

- 逻辑值: `0xAA55`
- 线上字节序: `0x55 0xAA`

### 2.3 CRC16

双向协议统一使用当前固件代码中的 CRC16 实现:

```cpp
using CRC16Modbus = crc::CRCX<16, 0x8005, 0xFFFF, true, true, 0x0000>;
```

- Poly: `0x8005`
- Init: `0xFFFF`
- RefIn: `true`
- RefOut: `true`
- XorOut: `0x0000`
- CRC 字段字节序: 小端

注意:

- 虽然代码别名沿用了 `CRC16Modbus`
- `crc.hpp` 注释示例里也把这组参数写成了 `CRC16/MODBUS`
- 但当前工程 `crc.hpp` 的查表实现，计算结果不等同于常见通用库里的标准 `CRC16/MODBUS`

上位机必须按本工程 `crc.hpp` 的实际算法实现，不能直接套用通用库的 `CRC16/MODBUS` 预设。

可用下面的参考向量做自检:

- 本工程 `crc.hpp` 对 ASCII `"123456789"` 的计算结果: `0xD641`
- 常见标准 `CRC16/MODBUS` 对 ASCII `"123456789"` 的结果: `0x4B37`

CRC 计算范围统一为:

- 从帧头 `SOF` 开始
- 到最后一个 payload 字节结束
- 不包含 CRC16 字段本身

## 3. 上位机 -> 下位机: 轨迹下发帧

### 3.1 帧结构

总长度: `43` 字节

| 偏移 | 长度 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | 2 | `uint16_t` | `sof` | 固定 `0xAA55`，线上为 `55 AA` |
| 2 | 2 | `uint16_t` | `index` | 轨迹点序号 |
| 4 | 4 | `float` | `q1` | 关节 1 目标角度 |
| 8 | 4 | `float` | `q2` | 关节 2 目标角度 |
| 12 | 4 | `float` | `q3` | 关节 3 目标角度 |
| 16 | 4 | `float` | `dq1` | 关节 1 目标角速度 |
| 20 | 4 | `float` | `dq2` | 关节 2 目标角速度 |
| 24 | 4 | `float` | `dq3` | 关节 3 目标角速度 |
| 28 | 4 | `float` | `ddq1` | 关节 1 目标角加速度 |
| 32 | 4 | `float` | `ddq2` | 关节 2 目标角加速度 |
| 36 | 4 | `float` | `ddq3` | 关节 3 目标角加速度 |
| 40 | 1 | `uint8_t` | `flags` | 末端状态与补偿标志 |
| 41 | 2 | `uint16_t` | `crc16` | 对前 41 字节按本工程 `crc.hpp` 算法做 CRC16 |

### 3.2 `flags` 定义

`flags` 当前仅使用高两位:

- `bit7 = 1`: 吸盘打开
- `bit7 = 0`: 吸盘关闭
- `bit6 = 1`: 负载补偿打开
- `bit6 = 0`: 负载补偿关闭
- `bit5..bit0`: 保留，发送时写 `0`

### 3.3 下位机接收行为

1. 下位机先按 `55 AA` 同步帧头。
2. 收满整帧后校验 CRC16。
3. CRC 错误时直接丢帧，不清空缓存，不回包。
4. CRC 正确时解析轨迹点并入队。

### 3.4 `index` 语义

- 当 `index == 0` 时，下位机会先清空当前轨迹队列，再将该点作为新轨迹起点处理。
- 下位机上电后收到的第一帧有效轨迹，也会触发一次轨迹队列清空。
- 因此建议每条新轨迹都从 `index = 0` 开始。

### 3.5 发送周期建议

当前固件轨迹接收周期默认按 `5 ms` 设计，建议上位机按 `200 Hz` 下发轨迹点。

如果下位机超过 `200 ms` 未收到新的有效轨迹点，则会进入超时保护:

- 保持当前关节位置目标
- 将速度和加速度目标置零

### 3.6 上位机组帧顺序

1. 按小端写入 `sof`
2. 依次写入 `index/q1/q2/q3/dq1/dq2/dq3/ddq1/ddq2/ddq3/flags`
3. 对前 `41` 字节按本工程 `crc.hpp` 算法计算 CRC16
4. 将 CRC 按小端写入帧尾
5. 发送总计 `43` 字节

### 3.7 示意帧

```text
55 AA | index_L index_H | q1(4) | q2(4) | q3(4) | dq1(4) | dq2(4) | dq3(4) | ddq1(4) | ddq2(4) | ddq3(4) | flags | crc_L crc_H
```

## 4. 下位机 -> 上位机: 状态反馈帧

### 4.1 帧结构

总长度: `32` 字节

| 偏移 | 长度 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | 2 | `uint16_t` | `sof` | 固定 `0xAA55`，线上为 `55 AA` |
| 2 | 4 | `float` | `q1` | 关节 1 当前角度 |
| 6 | 4 | `float` | `q2` | 关节 2 当前角度 |
| 10 | 4 | `float` | `q3` | 关节 3 当前角度 |
| 14 | 4 | `float` | `dq1` | 关节 1 当前角速度 |
| 18 | 4 | `float` | `dq2` | 关节 2 当前角速度 |
| 22 | 4 | `float` | `dq3` | 关节 3 当前角速度 |
| 26 | 4 | `float` | `pressure_kpa` | 压力值，单位 `kPa` |
| 30 | 2 | `uint16_t` | `crc16` | 对前 30 字节按本工程 `crc.hpp` 算法做 CRC16 |

### 4.2 下位机发送周期

当前固件每 `50 ms` 发送一次反馈帧，即约 `20 Hz`。

### 4.3 上位机接收行为建议

1. 按 `55 AA` 找帧头。
2. 收满 `32` 字节。
3. 校验帧尾 CRC16。
4. CRC 正确后再解析 `q1/q2/q3/dq1/dq2/dq3/pressure_kpa`。
5. CRC 错误时直接丢帧。

### 4.4 示意帧

```text
55 AA | q1(4) | q2(4) | q3(4) | dq1(4) | dq2(4) | dq3(4) | pressure_kpa(4) | crc_L crc_H
```

### 4.5 下位机反馈完整示例

假设下位机当前发送以下一帧反馈数据:

- `q1 = 1.0f`
- `q2 = 2.0f`
- `q3 = 3.0f`
- `dq1 = 0.5f`
- `dq2 = -0.5f`
- `dq3 = 0.0f`
- `pressure_kpa = 25.5f`

则前 30 字节 payload 如下:

```text
55 AA
00 00 80 3F
00 00 00 40
00 00 40 40
00 00 00 3F
00 00 00 BF
00 00 00 00
00 00 CC 41
```

对以上 30 字节执行本工程 `crc.hpp` 中的 CRC16 计算，结果为:

- CRC16 数值: `0x4081`
- 发送字节序: `81 40`

补充说明:

- 如果按常见标准 `CRC16/MODBUS` 计算，这一帧会得到 `0x519F`
- 该值对当前固件是错误的，不能使用

因此，下位机最终发出的完整 32 字节数据帧应为:

```text
55 AA 00 00 80 3F 00 00 00 40 00 00 40 40 00 00 00 3F 00 00 00 BF 00 00 00 00 00 00 CC 41 81 40
```

上位机在串口中应接收到完全一致的 32 字节数据。CRC 校验通过后，解包结果应为:

- `sof = 0xAA55`
- `q1 = 1.0`
- `q2 = 2.0`
- `q3 = 3.0`
- `dq1 = 0.5`
- `dq2 = -0.5`
- `dq3 = 0.0`
- `pressure_kpa = 25.5`
- `crc16 = 0x4081`

## 5. 建议的上位机实现要点

- 不要按结构体内存镜像以外的字节序发送，必须严格按小端打包。
- `float` 必须使用 32 位单精度。
- 每条新轨迹首帧建议发送 `index = 0`。
- `flags` 中未定义位保持为 `0`。
- 上位机和下位机都应在 CRC 失败时丢弃整帧，不做半帧复用。
- 当前协议没有 ACK/NACK 机制，也没有单独错误码返回。

## 6. 版本说明

本文档对应 CRC16 改造后的协议版本。

- 轨迹下发帧: 由原 `41` 字节改为 `43` 字节
- 状态反馈帧: 由原 `30` 字节改为 `32` 字节

旧版上位机与当前固件协议不兼容，必须同步升级。
